package com.leetcode.binary_tree;

public class BinaryTreeGenerator<T> {
    public static <T> TreeNode generateBinaryTree(T[] arr) {
        TreeNode node = new TreeNode();

        for (int i = 0; i < arr.length; i++) {

        }
        return null;
    }
}
